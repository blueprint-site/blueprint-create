/** @type {import('tailwindcss').Config} */
export default {
  // This is an empty config file to ensure compatibility
  // The actual configuration is handled by @tailwindcss/vite in vite.config.ts
};